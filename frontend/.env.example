# Copy this to .env and fill in values.
# VITE_ prefix is required for variables exposed to the browser.

# URL of the backend API (default: http://localhost:3001)
VITE_API_URL=http://localhost:3001
